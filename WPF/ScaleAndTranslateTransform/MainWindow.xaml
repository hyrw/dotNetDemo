<Window x:Class="ScaleAndTranslateTransform.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScaleAndTranslateTransform"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="重置变换" Click="ResetButton_Click" Padding="10,5" Margin="5"/>
        </StackPanel>

        <!-- Grid 容器 -->
        <Grid Grid.Row="1" ClipToBounds="True" Background="LightGray" 
          MouseWheel="Grid_MouseWheel" 
          MouseLeftButtonDown="Grid_MouseLeftButtonDown" 
          MouseMove="Grid_MouseMove" 
          MouseLeftButtonUp="Grid_MouseLeftButtonUp">
            <Grid x:Name="EleContainer">
                <Grid.RenderTransform>
                    <MatrixTransform />
                </Grid.RenderTransform>
                <Image
               Width="400" Height="300"
               HorizontalAlignment="Center" VerticalAlignment="Center"
               Source="pack://application:,,,/Resources/cat.jpeg"
               Stretch="UniformToFill"/>
                <Rectangle Width="50" Height="60" Fill="Red" />
            </Grid>
        </Grid>
    </Grid>
</Window>
